apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vault-ui-ingress
  namespace: common
spec:
  ingressClassName: traefik
  rules:
  - host: "vault-ui.karim-portfolio.xyz"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: vault
            port:
              number: 8200